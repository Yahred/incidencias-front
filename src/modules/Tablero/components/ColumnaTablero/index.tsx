import { styled } from "@mui/material";

const ColumnaTablero = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  gap: '4px',
  alignItems: 'flex-start',
});

export default ColumnaTablero;
