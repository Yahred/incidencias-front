// Mensajes
export const CAMPO_REQUERIDO = 'El campo es requerido.';
export const EMAIL_INVALIDO = 'El email no tiene un formato válido.'
export const CARACTERISTICA_REPETIDA = 'La característica está repetida';
export const CAMPO_NUMERICO = 'El valor debe ser númerico.'

// Regex
export const EMAIL_REGEX = /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/;
